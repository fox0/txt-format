{
  "name": "txt-format",
  "displayName": "txt-format",
  "description": "",
  "version": "0.0.1",
  "repository": "https://github.com/fox0/txt-format",
  "engines": {
    "vscode": "^1.97.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "txt-format.txt",
        "title": "Викифицировать"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "dependencies": {
    "eyo-kernel": "^2.5.6"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@types/node": "20.x",
    "@types/vscode": "^1.97.0",
    "@vscode/vsce": "^3.2.2"
  }
}
