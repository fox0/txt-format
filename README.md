# Плагин для VS Code «Типографика и ёфикатор»

```bash
git clone https://github.com/fox0/txt-format
npm install
npm run compile
npx @vscode/vsce package
code --install-extension txt-format-*.vsix
```
